// Stub only — this file exists to give the power-skill-sandbox scenario a real
// contract-shaped path signal (matches path_glob_categories.contract_globs). It is never
// meant to run; see ../../../SCENARIO.md.
export function cancelOrder() {}
