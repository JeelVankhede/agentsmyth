// Stub only — this file exists to give the power-skill-sandbox scenario a real UI-shaped path
// signal (matches path_glob_categories.ui_globs). Never meant to run; see ../../../SCENARIO.md.
export function CancelOrderDialog() { return null; }
