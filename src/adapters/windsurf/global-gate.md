# agentsmyth global gate BEGIN
agentsmyth lifecycle: when repo has workflow/ dir, load ~/.agentsmyth/workflow/router.md + agent-behavior.yaml before any task. Per-repo config in workflow/config/. Run `agentsmyth check` to bootstrap if absent. Phase skills at ~/.agentsmyth/workflow/skills/.
# agentsmyth global gate END
