version: 1
kind: open-items
items:
  - id: OI-1
    source: follow-up
    owner: user (to schedule)
    next_action: "Design + implement Wave 3 (C1-C3 explorers, D1-D7 domain experts, E1 verification-parallelizer) — narrowed from the original Wave 2-4 scope now that Wave 2 (B1, B2, B6, B9, E2) has shipped"
    status: done
    first_seen_run: power-skills-spine-v1
  - id: OI-2
    source: follow-up
    owner: user
    next_action: "Run a real Standard/Complex task through the Wave 1 invariant spine to measure actual drift reduction, per the resolved spec's own §8 checkpoint, before starting Wave 2–4"
    status: done
    first_seen_run: power-skills-spine-v1
  - id: OI-3
    source: follow-up
    owner: user
    next_action: "Decide whether/when to open a PR for this Wave 0+1 chain (target feat/system-level-install, per the branch decision)"
    status: done
    first_seen_run: power-skills-spine-v1
  - id: OI-4
    source: follow-up
    owner: user/agent
    next_action: "Audit other shipped validators for the same \"passed its fixture, never tested against a real artifact\" risk class found in check-scope-fence/check-manifest-coverage/check-release-readiness"
    status: done
    first_seen_run: power-skills-spine-v1
  - id: OI-5
    source: follow-up
    owner: user
    next_action: "Watch for recurrence of the mid-session unexplained auto-staging anomaly; investigate if it happens again"
    status: open
    first_seen_run: power-skills-spine-v1
  - id: OI-6
    source: follow-up
    owner: user (to schedule)
    next_action: "Design + implement Wave 4 (B4, remaining playbooks)"
    status: done
    first_seen_run: power-skills-wave2-v1
  - id: OI-7
    source: follow-up
    owner: user
    next_action: "Decide whether/when to open a PR for the Wave 2 chain (target feat/wp-r4-power-skills-spine or main, re-check origin/main staleness at that time per this chain's own R8 lesson)"
    status: done
    first_seen_run: power-skills-wave2-v1
  - id: OI-8
    source: follow-up
    owner: user/agent
    next_action: "Investigate the recurring range-shorthand habit (3 occurrences across 2 chains) with a structural check, rather than relying on Review noticing it again"
    status: open
    first_seen_run: power-skills-wave2-v1
  - id: OI-9
    source: follow-up
    owner: user
    next_action: "Consider whether this per-work-package branching workflow needs a standing \"fetch + check origin/main before Ship\" step, given R8's stale-main root cause"
    status: open
    first_seen_run: power-skills-wave2-v1
  - id: OI-10
    source: follow-up
    owner: user
    next_action: "Decide whether/when to open a PR for the Wave 3 chain (target main, re-check origin/main staleness at that time per Wave 2's R8 lesson) — resolved: main was already byte-identical to the Wave 3 branch by the time this was checked (cause undetermined), so no PR was needed"
    status: done
    first_seen_run: power-skills-domain-experts-v1
  - id: OI-11
    source: follow-up
    owner: user/agent
    next_action: "Add a Plan-phase cross-check (spec card phase list vs. declared Touches) to catch a D3-class wiring gap earlier than Build's own grep -l verification"
    status: open
    first_seen_run: power-skills-domain-experts-v1
  - id: OI-12
    source: follow-up
    owner: user/agent
    next_action: "Update Ship's own default behavior to ask \"resolved fix vs. genuine waiver\" before presenting a checkpoint — now a confirmed repeating pattern (Wave 2's R8, this chain's E1/D3 items)"
    status: open
    first_seen_run: power-skills-domain-experts-v1
  - id: OI-13
    source: follow-up
    owner: user
    next_action: "Consider a periodic (not per-chain) manual quality pass across all 39 D1-D7 route files, since only 21 were directly spot-read during this chain's Review"
    status: open
    first_seen_run: power-skills-domain-experts-v1
  - id: OI-14
    source: follow-up
    owner: user
    next_action: "Decide whether/when to open a PR for the Wave 4 chain (target main)"
    status: open
    first_seen_run: power-skills-wave4-v1
  - id: OI-15
    source: follow-up
    owner: user/agent
    next_action: "Consider the \"mark artifact approved before real user review\" self-check as a structural habit, given it recurred 3 times within Wave 4 alone"
    status: open
    first_seen_run: power-skills-wave4-v1
  - id: OI-16
    source: follow-up
    owner: user
    next_action: "WP-R4 is now fully complete (all 4 waves, 26 total skills) — decide what initiative comes next"
    status: open
    first_seen_run: power-skills-wave4-v1
  - id: OI-17
    source: follow-up
    owner: user
    next_action: "Merge PR #29 (feat/power-skill-sandbox), then retarget/merge PR #30 to main"
    status: open
    first_seen_run: wp-r5-repo-shape-taxonomy-v1
  - id: OI-18
    source: follow-up
    owner: user
    next_action: "Design proposal written (Notion, 2026-07-12): agentsmyth init --workspace-root <path> flag, orthogonal to --system, with a first-member-vs-joining fork on whether <path>/workflow/ already exists. Deliberately not implemented -- no real polyrepo-member use case exists yet to build it against. Revisit the proposal (don't re-derive) when one does."
    status: open
    first_seen_run: wp-r5-repo-shape-taxonomy-v1
  - id: OI-19
    source: follow-up
    owner: user/agent
    next_action: "Done: test/run-root-resolution-drift-tests.mjs (npm run root-resolution:test) spawns lib.mjs, check-setup-complete.mjs, and bin/agentsmyth.mjs as real subprocesses against 4 scenarios and asserts they agree. Found and fixed a real bug while building it: check-setup-complete.mjs used process.env.HOME instead of homedir() for tilde-expansion."
    status: done
    first_seen_run: wp-r5-repo-shape-taxonomy-v1
  - id: OI-20
    source: follow-up
    owner: user
    next_action: "First real polyrepo-member configuration should explicitly verify resolveGitCwd() end-to-end and report back — no fixture currently exercises this"
    status: open
    first_seen_run: wp-r5-repo-shape-taxonomy-v1
  - id: OI-22
    source: follow-up
    owner: user/agent
    next_action: "Harden check-manifest-coverage.mjs/check-coverage-ledger.mjs/check-phase-map.mjs's manifest-ID extraction regex -- found 3 false positives/negatives in the WP-R7 chain alone: matched inside a hyphenated compound token, matched inside plain prose, and failed to recognize a parenthetical qualifier as covering its ID. Done: shipped as PR #36 (feat/manifest-id-parser-hardening) -- structured-tag scanning + shared parseIdList() helper + narrower waiverIds() exclusion, 3 new conformance fixtures now CI-enforced. Note: this entry was reconstructed from the brief's quoted original text -- the 'real' OI-22 (added on the still-unmerged feat/init-prepare-interop, PR #35) will collide with this one at merge time; keep this done version, see manifest-id-parser-hardening-v1 Reflect Follow-Ups."
    status: done
    first_seen_run: init-prepare-interop-v1
  - id: OI-24
    source: follow-up
    owner: user
    next_action: "Reconcile open-items.yaml OI-22 duplication between PR #35 (adds OI-21/22/23, OI-22 left open) and PR #36 (recreates OI-22 as done) once both merge to main -- keep the done version, verify no other ID collisions"
    status: open
    first_seen_run: manifest-id-parser-hardening-v1
  - id: OI-25
    source: follow-up
    owner: user
    next_action: "Decide whether/when to merge PR #35 (feat/init-prepare-interop, still open) and PR #36 (feat/manifest-id-parser-hardening)"
    status: open
    first_seen_run: manifest-id-parser-hardening-v1
  - id: OI-26
    source: follow-up
    owner: user/agent
    next_action: "Audit package.json's other npm run *:test scripts for the same CI-unenforced gap found in conformance:test (only violations:test was wired into ci.yml before this chain)"
    status: open
    first_seen_run: manifest-id-parser-hardening-v1
  - id: OI-27
    source: follow-up
    owner: user/agent
    next_action: "Consider adding 'verify a fix's boundary with a before/after comparison before implementing' as an explicit step in lifecycle-build's Workflow section, per this chain's Phase 3/Phase 6 pattern"
    status: open
    first_seen_run: manifest-id-parser-hardening-v1
