version: 1
kind: finding-quality

# Active finding-quality ledger. One row per council finding, written at Review with
# outcome: pending, settled at Test, Ship or Reflect. A row that closes moves to
# finding-quality-archive.yaml in the same operation, so a row is present in exactly one file.
#
# Any figure computed from this ledger must read BOTH files. A count from this one alone reports
# the current cycle rather than the accumulated baseline.
#
# What remains here is the WP-R18 Review council's five UNSETTLED findings. The other 72 closed
# proved-real and rotated to the archive in the same operation, which is why this file is short
# and the archive is not — do not read the length of this file as the size of that council.
#
# Each row below names the phase that owns it. None is an oversight: three are a Ship-owned
# external handoff, one is a Reflect-owned ledger rotation that must FOLLOW a fix made in this
# remediation rather than precede it, and one needs a platform no council member had.
items:
  - id: FQ-63
    finding_id: F7
    source_artifact: workflow/artifacts/reviews/wp-r18-delta-upgrades-v1.md
    first_seen_run: wp-r18-delta-upgrades-v1
    disposition: accepted
    outcome: pending
    # deferred to reflect: OI-105 ledger closure — Reflect owns rotation, and it must follow the marker-check fix rather than precede it
  - id: FQ-80
    finding_id: F24
    source_artifact: workflow/artifacts/reviews/wp-r18-delta-upgrades-v1.md
    first_seen_run: wp-r18-delta-upgrades-v1
    disposition: accepted
    outcome: pending
    # deferred to test: RI18 eight-entry branch needs a non-darwin platform no council member could run on; carried as the council surviving item
