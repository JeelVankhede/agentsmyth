# Token Map (fixture — all valid; the seeded drift lives in config-map.md)

| Token | Config | Field |
|---|---|---|
| `{{DEFAULT_BRANCH}}` | `workflow/config/repo-profile.yaml` | `repository.default_branch` |
